import { Routes } from '@angular/router';
import { HomeComponent } from '../pages/home/home.component';

const dashboardRoutes: Routes = [{ path: '', component: HomeComponent }];

export default dashboardRoutes;
